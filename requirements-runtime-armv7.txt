# armv7-specific runtime deps (subset — no cryptography wheels available for
# armv7 via manylinux, and psycopg[binary] doesn't ship an armv7 wheel).
#
# Regenerate the lock:
#   pip-compile --generate-hashes --output-file=requirements-runtime-armv7.lock \
#               requirements-runtime-armv7.txt

aiohttp==3.14.1
redis==8.0.1
maxminddb==3.1.1
psycopg[c]==3.3.4
PyJWT==2.13.0
