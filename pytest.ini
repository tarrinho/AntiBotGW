[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function

# Tests excluded from CI (require a live proxy container or external services):
#   test_functional.py        — spins up proxy + upstream, hangs without container
#   test_integration.py       — same
#   test_dashboard_data.py    — same
#   test_v14.py               — same
#   test_timescaledb_soak.py  — requires a running TimescaleDB container
